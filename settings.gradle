pluginManagement {
    repositories {
        maven { url "https://maven.fabricmc.net/" }
        maven { url "https://maven.nanite.dev/releases" }
        gradlePluginPortal()
    }
}

include "common", "fabric"

rootProject.name = "proxima-hammers"
